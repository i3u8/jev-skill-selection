name: jev-skill-selection
version: 0.2.0
description: Pre-LLM keep/drop filter for Hermes skills (hard filter via llm_request; soft context optional).
entry: __init__.py
hooks:
  - pre_llm_call
provides_hooks:
  - pre_llm_call
provides_middleware:
  - llm_request
