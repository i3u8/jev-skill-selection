---
name: git-ops
description: Git branching, commits, rebases, and pull request workflows.
tags: [git, github, pr, commit]
---

# Git Ops

Use this skill when the user needs help with git history, branches, or opening PRs.

## Steps
1. Inspect `git status` and `git log`.
2. Prefer non-destructive commands; never force-push main unless asked.
3. Draft clear commit messages focused on why.
